add haxe to ag/ack

binary to organize haxe notes
  hxn, uses cln stuff + useful other binary stuf fprobably

Instantiation
  Object#initialize
    takes no params, does nothing
  new
    returns a RObject with klass set to self
    initializes the object, passing the params
  allocate
    makes an RObject with the klass set

self

Instance variables
  instance variable gets saved and looked up on self

Instance Methods with explicit target
  get invoked on self
  when they call methods with imlicit target, self is unchanged
  receiving ordinal params


Object#puts <-- put it here for now
  keeps some sort of internal queue of printed shit?
