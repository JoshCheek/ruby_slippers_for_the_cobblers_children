// https://docs.npmjs.com/files/package.json
{ "name":         "ruby_slippers_for_the_cobblers_children",
  // "version":      "0.0.1",
  "dependencies": {
  },
  "scripts": {
    "test": "gulp test"
  }
}
